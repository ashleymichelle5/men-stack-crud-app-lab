<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cars Index</title>
  </head>
  <body>
    <a href="/cars/new">Add New Car</a>
    <h1><%= title %></h1>
    <ul>
      <% allCars.forEach(car => { %>
      <li>
        <a href="/cars/<%= car._id %>">
          <%= car.brand %> - <%= car.year %> <%= car.isAvailable === true
          ? 'is' : 'is not' %> available 
        </a>
      </li>
      <% }) %>
    </ul>
  </body>
</html>